<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/lite_blue"
    android:orientation="vertical"
    tools:context=".activity.SheeListActivity">
    <include
        android:id="@+id/attentoolbarMain"
        layout="@layout/attentoolbar"/>

    <TextView
        android:id="@+id/txv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:text="There are no any Attendance list available"
        android:textSize="@dimen/_20sdp"
        android:layout_centerInParent="true"
        android:layout_marginStart="@dimen/_50sdp"
        android:layout_marginEnd="@dimen/_50sdp"
        android:gravity="center"
        android:textColor="@color/grey"/>

        <ListView
            android:id="@+id/sheetList"
            android:layout_below="@+id/attentoolbarMain"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"/>

</RelativeLayout>